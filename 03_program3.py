
# program to print the are if triangle 

base = float(input("Enter the base of tiangle:"))
height = float(input("Enter the hight of tiangle:"))

area = 0.5 * base * height
print("Area of the triangle is", area)